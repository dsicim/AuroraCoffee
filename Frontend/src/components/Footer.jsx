export default function Footer() {
  return (
    <footer className="border-t border-[var(--aurora-border)] px-6 py-8 text-sm text-[var(--aurora-text)] lg:px-10">
      <div className="mx-auto flex max-w-7xl flex-col gap-3 md:flex-row md:items-center md:justify-between">
        <p>Aurora Coffee Roastery. Crafted for the first frontend milestone.</p>
        <p>React + Tailwind starter for the store team.</p>
      </div>
    </footer>
  )
}
